type Props = {
    noteContent: string;
};
export declare const useMermaid: ({ noteRenderedBody, }: {
    noteRenderedBody?: string;
}) => void;
export declare function DendronNote({ noteContent, ...rest }: Props): JSX.Element;
export {};
