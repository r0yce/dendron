/** Ambient types for textextensions (ESM package; CJS interop under TS 7 bundler resolution). */
declare module "textextensions" {
  const textExtensions: string[];
  export default textExtensions;
}
